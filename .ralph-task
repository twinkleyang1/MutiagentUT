#!/bin/bash
# Ralph-Loop Task: Multi-Agent UT Generation

# Task Description
# Run the Multi-Agent UT Generation System to automatically generate
# unit tests for Java projects. The system uses Planner, Generator,
# and Evaluator agents working together.

# Working Directory
cd /home/twinkle/app/808/Agent_UT/MutiagentUT

# Command to Run
# python main.py --java-project-path /home/twinkle/app/808/Agent_UT/MutiagentUT/Test --max-iterations 50

# Promise
# CODE_IMPROVED

# Instructions for ralph-loop agent:
# 1. Ensure the Agent directory structure exists with all subdirectories
# 2. Verify all Python files are in place (planner, generator, evaluator, shared)
# 3. If tests are generated in Test/ directory, run evaluation
# 4. Check progress.txt for current status
# 5. Continue iterations until max-iterations reached or all classes tested
# 6. Output <promise>CODE_IMPROVED</promise> when complete

# Max Iterations
MAX_ITER=50

echo "=== Ralph-Loop: Multi-Agent UT Generation ==="
echo "Max iterations: $MAX_ITER"
echo "Working directory: $(pwd)"